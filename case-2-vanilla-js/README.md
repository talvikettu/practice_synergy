# Case 2: Vanilla JS Counter

Open `index.html` in a browser.

The app includes:

- plus/minus buttons with event listeners;
- gradient styling and centered layout;
- dynamic color of the result box for positive, negative, and zero values;
- disabled buttons at `10` and `-10`;
- an extra status message for extreme values.
